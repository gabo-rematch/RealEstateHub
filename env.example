# UAE Property Portal Environment Variables

# ==============================================
# SUPABASE CONFIGURATION (Required)
# ==============================================
# Get these from your Supabase project dashboard:
# https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api

# Your Supabase project URL
SUPABASE_URL=https://your-project-id.supabase.co

# Supabase anonymous public key (safe to expose in client)
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase service role key (server-side only, more permissions)
# Optional: Only needed for server-side operations requiring elevated permissions
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# ==============================================
# WEBHOOK CONFIGURATION (Required)
# ==============================================
# URL where inquiry submissions will be sent
# This must be a valid HTTPS endpoint that accepts POST requests
VITE_WEBHOOK_URL=https://your-webhook-endpoint.com/inquiries

# ==============================================
# APPLICATION CONFIGURATION (Optional)
# ==============================================
# Server port (defaults to 5000)
PORT=5000

# Node environment (development|production)
NODE_ENV=development

# ==============================================
# SECURITY CONFIGURATION (Optional)
# ==============================================
# CORS allowed origins (comma-separated for multiple)
CORS_ORIGIN=http://localhost:3000,http://localhost:5000

# Session secret for any future session management
SESSION_SECRET=your_random_session_secret_here 